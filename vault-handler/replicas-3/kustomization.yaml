apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vault

resources:
  - job.yaml
  - rbac.yaml

images:
  - name: kubefirst/vault-handler
    newName: ghcr.io/konstructio/vault-handler
    newTag: 'v1.0.0'

generatorOptions:
  disableNameSuffixHash: true
